doctype html
html(lang="en" ng-app)
	head
		meta(charset="utf-8")
		meta(name="viewport" content="width=device-width, initial-scale=1")

		title Histories

	body
		// APP CONTAINER
		#app
			
			// HEADER
			header.header.header-1
				router-link(to="/home") Histories
				router-link(to="/stories") Stories

			// APP CONTENT
			transition(name="fade")
				router-view#app-content
